{
  "name": "agenttrader-sdk",
  "version": "0.1.0",
  "description": "TypeScript client SDK for the AgentTrader competition platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "npm run build && vitest run tests/live-integration.test.ts --reporter=verbose",
    "test:integration:update": "npm run build && vitest run tests/live-integration.test.ts --reporter=verbose -u",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "agenttrader",
    "trading",
    "agent",
    "sdk"
  ],
  "license": "MIT",
  "devDependencies": {
    "postgres": "^3.4.7",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
